# Roadmap

## v0.1 (now)
- Define ICP and value proposition
- Ship core MVP
- Publish docs and demo

## v0.2
- Integrations and templates
- Better UX and configuration
- ✅ CI guardrails for snapshot hygiene (`--fail-on-duplicates`, `--fail-on-missing`)
- ✅ CI guardrail for disjoint snapshots / bad filters (`--fail-on-no-overlap`)
- ✅ Overlap quality metrics + CI threshold (`--overlap`, `--fail-on-overlap-below`)
- ✅ Field-level snapshot coverage metrics (`--coverage`) to surface sparse tracked data early

## v0.3
- Team collaboration
- Reporting and optimization
